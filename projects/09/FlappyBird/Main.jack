
/** Initializes a new Flappy Bird game game and starts running it. */
class Main {
    function void main() {
        var FlappyBirdGame game;
        let game = FlappyBirdGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
